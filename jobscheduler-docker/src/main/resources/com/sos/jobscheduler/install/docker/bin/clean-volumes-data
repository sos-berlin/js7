#!/usr/bin/env bash
set -e

cd "$(cd "$(realpath "${0%/*}")"/../../build/bin/../.. && pwd)"

rm -rf volumes/{master,backup-master,provider,agent-1,agent-2}/data/*
rm -rf volumes/testMasterAgent/master/data/*
rm -rf volumes/testMasterAgent/AGENT-*/data/*
