#!/usr/bin/env bash
set -e

[ -d volumes/master/config ] || {
  echo Missing directory volume/master/config
  exit 1
}
rm -rf volumes/{master,agent-1,agent-2}/data
mkdir -p volumes/{master,agent-1,agent-2}/data/{logs,state,tmp}
