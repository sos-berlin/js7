#!/usr/bin/env bash
set -e

hprof=/var/opt/jobscheduler/master/data/logsOutOfMemoryError.hprof
rm -fv "$hprof"

exec /opt/jobscheduler/bin/master \
  -java-option="-XX:+HeapDumpOnOutOfMemoryError" \
  -java-option="-XX:HeapDumpPath=$hprof" \
  "$@"
