#!/usr/bin/env bash
set -euo pipefail

bin/kill-and-restart-controller-monkey &
controllerKillMonkey=$!

bin/kill-and-restart-subagents-monkey &
agentKillMonkey=$!

trap "kill -- -$$ >/dev/null || kill $controllerKillMonkey $agentKillMonkey" EXIT
wait
