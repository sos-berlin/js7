#! /usr/bin/env bash
set -euo pipefail

cd "$(cd "$(realpath "${0%/*}")"/../../build/bin/../.. && pwd)"

# First, free some disk space
rm -rf volumes/*/data/logs

# Fails if disk is full:
docker-compose kill
build/bin/clean-volumes-data
