#!/usr/bin/env bash
set -euo pipefail

cd "$(cd "$(realpath "${0%/*}")"/../../build/bin/../.. && pwd)"

rm -rf volumes/{controller,backup-controller,provider,subagent-1-a,subagent-1-b,subagent-2-a}/data/*
rm -rf volumes/testControllerAgent/controller/data/*
rm -rf volumes/testControllerAgent/AGENT-*/data/*

# Keep 'https' directory with CA certificate
